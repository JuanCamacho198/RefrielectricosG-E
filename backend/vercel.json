{
  "version": 2,
  "ignoreCommand": "git diff HEAD^ HEAD --quiet -- ./",
  "builds": [
    {
      "src": "src/main.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "src/main.ts"
    }
  ]
}
